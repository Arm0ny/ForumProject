import { MarkdownPipePipe } from './markdown-pipe.pipe';

describe('MarkdownPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MarkdownPipePipe();
    expect(pipe).toBeTruthy();
  });
});
