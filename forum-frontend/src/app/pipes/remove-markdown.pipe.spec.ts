import { RemoveMarkdownPipe } from './remove-markdown.pipe';

describe('RemoveMarkdownPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveMarkdownPipe();
    expect(pipe).toBeTruthy();
  });
});
