import { TitlesShortenerPipe } from './titles-shortener.pipe';

describe('TitlesShortenerPipe', () => {
  it('create an instance', () => {
    const pipe = new TitlesShortenerPipe();
    expect(pipe).toBeTruthy();
  });
});
